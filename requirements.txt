# OpenMC_NIUQ – Python dependencies
#
# Install with:
#   conda create -n openmc-env -c conda-forge openmc
#   conda activate openmc-env
#   pip install -r requirements.txt

# Neutron transport (install via conda-forge, not pip):
#   openmc >= 0.14

# Uncertainty quantification
easyvvuq >= 1.2
chaospy >= 4.3

# Parallel execution via QCG Pilot Job Manager
# Required only when using --exec-mode spack
# qcg-pilotjob uses a LocalManager that works on a single workstation
# as well as inside a SLURM allocation (reads SLURM_NTASKS automatically).
qcg-pilotjob >= 0.14
easyqcgpj >= 0.4

# General scientific stack
numpy
pyyaml
matplotlib
